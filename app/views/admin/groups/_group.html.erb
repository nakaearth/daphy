<div id="<%= group.id %>" class="group">
  <div id="group_menu">
    <%= link_to '詳細', admin_group_path(group) %>&nbsp;&nbsp;|
    <%= link_to '編集', edit_admin_group_path(group) %> &nbsp;&nbsp;|
    <%= link_to '削除', admin_group_path(group), method: :delete, data: { confirm: 'Are you sure?' } %> &nbsp;&nbsp;|
  </div>
  <br/>
  <%= group.name %>
  <br/>
  <%= group.created_at %>
</div>

<script type='text/javascript'>
  $("#<%=group.id %>").draggable({
    snapMode: 'inner',
    containment: '#job_board',
    revert: 'true'
   })
</script>
